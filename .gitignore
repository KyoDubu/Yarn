# Python
.venv/
venv/
__pycache__/
*.pyc

# Env / secrets
.env
*.env
serviceAccount*.json

# Node (we do not use it, but just in case)
node_modules/

# Built artifacts, if/when build.py / build_pages.py materialise (see DEVLOG -
# they don't exist yet, so yarn.html below is REAL hand-written source, not
# an artifact - do not add it here).
docs/

# OS noise
Thumbs.db
desktop.ini
.DS_Store

# Personal character data - never commit real save files
yarn-backup-*.json
my_yarn_backup.json
